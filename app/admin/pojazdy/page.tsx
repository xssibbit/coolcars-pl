import Link from "next/link";
import { requireAdmin } from "@/lib/auth"; import { db } from "@/lib/db"; import { AdminShell } from "@/components/AdminShell"; import { DeleteVehicleButton } from "@/components/DeleteVehicleButton"; import { Icon } from "@/components/Icons"; import { formatKm, formatPln } from "@/lib/format";
export const metadata={title:'Pojazdy — Admin'};
export default async function AdminVehicles(){await requireAdmin();const vehicles=await db.vehicle.findMany({orderBy:{updatedAt:'desc'}});return <AdminShell><div className="section-head" style={{marginBottom:22}}><div><h1>Pojazdy</h1><p className="dashboard-sub" style={{margin:0}}>Edytuj dane, cenę, przebieg i status publikacji.</p></div><Link href="/admin/pojazdy/nowy" className="btn btn-accent">+ Dodaj pojazd</Link></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Nr</th><th>Pojazd</th><th>Rok</th><th>Przebieg</th><th>Cena netto</th><th>Status</th><th>Akcje</th></tr></thead><tbody>{vehicles.map(v=><tr key={v.id}><td>{v.stockNumber}</td><td className="table-title">{v.title}</td><td>{v.year}</td><td>{formatKm(v.mileage)}</td><td>{formatPln(v.priceNet)}</td><td><span className="mini-status">{v.status}</span></td><td><div className="admin-actions"><Link className="icon-btn" title="Edytuj" href={`/admin/pojazdy/${v.id}/edytuj`}><Icon name="edit" size={15}/></Link><DeleteVehicleButton id={v.id}/></div></td></tr>)}</tbody></table></div></div></AdminShell>}
